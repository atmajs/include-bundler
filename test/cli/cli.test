const OUTPUT_DIR = 'test/cli/temp/';

UTest({
    $before () {
        io.Directory.remove(OUTPUT_DIR)
    },
    'should run builder' () {

        let { execFileSync } = require('child_process');


        execFileSync('test/cli/fixtures/index.html')
    }
})